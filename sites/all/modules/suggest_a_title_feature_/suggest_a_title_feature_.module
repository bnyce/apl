<?php
/**
 * @file
 * Code for the Suggest a Title (Feature) feature.
 */

include_once 'suggest_a_title_feature_.features.inc';

function suggest_a_title_feature__form_alter(&$form, &$form_state, $form_id) {
  if ($form_id == 'suggest-a-title-node-form') {
    drupal_set_title('Suggest a Title for Purchase');
  }
}
